import { TermTitle } from 'src/modules/TermTitle';

import styles from './styles.module.css';

export const DownSide = () => {
    return (
        <section className={styles.container}>
            <TermTitle />
        </section>
    );
};
